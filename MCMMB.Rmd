---
title: "R Notebook"
output: html_notebook
---

This is an [R Markdown](http://rmarkdown.rstudio.com) Notebook. When you execute code within the notebook, the results appear beneath the code. 

Try executing this chunk by clicking the *Run* button within the chunk or by placing your cursor inside it and pressing *Ctrl+Shift+Enter*. 

```{r}
plot(cars)
```

Add a new chunk by clicking the *Insert Chunk* button on the toolbar or by pressing *Ctrl+Alt+I*.

When you save the notebook, an HTML file containing the code and output will be saved alongside it (click the *Preview* button or press *Ctrl+Shift+K* to preview the HTML file).

The preview shows you a rendered HTML copy of the contents of the editor. Consequently, unlike *Knit*, *Preview* does not run any R code chunks. Instead, the output of the chunk when it was last run in the editor is displayed.


# Order:

Skript reihenfolge:

# To find the species to be used for our analysis we used the loops in bird_plot_loops.R to plot all species from the gbif and IUCN datasets to find firds with sufficient observation points and different ranges to be used for our project.

1. MCMMB_Main: (alle librarys, laden der daten IUCN (“birdlife”) , “species” =alle arten aus GBIF = nicht mehr nötig?, berreinigen der GBIF daten/anpassen der Kartengröße und selektion auf 100 präsens Punkte pro Art)
umbenenen, aufteielen? oder so ok?
#,echo=FALSE}

```{r}
source("MCMMB_Main.R")
# get regio from here
```

2. MCMMB_2: Indikatoren, erstellen der Rasterkarten
# rename -> predictors?

The Indicator.R script is used to read in data for the various indicators to be used for the SDM. These are then cropped to the target region and saved in .tif format in the folder data/indicator_stack/ for later use. As the process for some of the bigger files takes a lot of time, ready to use indicator .tif files are to be found in the aformentioned folder.

```{r}
# source("Indicator.R")
# get elevation and tif_predictors from here?
```

3. The script species_loops.R includes multiple functions for plotting all species in indonesia found in gbif, one for plotting all species found in IUCN birdlife. It includes two functions each for creating lists with the input bird species data for gbif ("get_gbif_birds(bird_names,gbif_crop,regio)") and IUCN ("get_iucn_birds(bird_names,birdlife,regio)") respectively : Erstellung der Karten der Verbreitungen der einzelnen Vogelart → heraussuchen welche Familie passt = Pachycephala
```{r}
source("species_loops.R")

bird_names = c("Casuarius bennetti", "Casuarius casuarius", "Casuarius unappendiculatus")
bird_names = c("Pachycephala lorentzi", 
               "Pachycephala meyeri", 
               "Pachycephala schlegelii",
               "Pachycephala simplex",
               "Pachycephala soror")

iucn_birds = 

# create plots of all birds and decide upon birdnames
# birdnames = c("bird1","bird2",...etc)
# ... bird_list = species_loops("birdnames")
```

4. Presence_absence: Erstellen einer Rasterkarte, dessen Werte 1 für presence und für absence =0 sind, die absence wurden zufällig erstellt und sind auch zufällig auf der Karte verteilt

While biomod2 also chas capabilities to create pseudo-absence data, we build a script to do it.

```{r}
source("presence_absence.R")
presence_absence_list = create_pseudo_absence(regio,gbif_birds)
#presence_absence_list = create_pseudo_absence(regio,bird_list)
#presence_absence_list = create_pseudo_absence(Elevation,regio,bird_list)

```

5. Sdm_biomod2: integrieren der Presence_absence daten, der Indikatoren (Karten) und Kalkulation der SDM´s

```{r}
source("sdm_biomod2.R")
test_sdms = calculate_sdm(presence_absence_list, tif_predictors)
```

6. plotting and comparing sdms with birdlife data 

```{r}
source("plotting_sdms.R")
#plot some nice plots here!
```






xxx.Example for how to attach code:

### 7.2 Code

MCMM

Mardown script auch hinzufügen!? und/oder mit abgeben

photosynthesis_tests_loop.R
```{r, eval = FALSE, code = xfun::read_utf8("an_gs_scripts/photosynthesis_tests_loop.R"), class.source = "fold-hide"}
```
setup_Photosynthesis_StomatalConductance.R
```{r, eval = FALSE, code = xfun::read_utf8("an_gs_scripts/setup_Photosynthesis_StomatalConductance.R"), class.source = "fold-hide"}
```
fun_calc_an_gs.R
```{r, eval = FALSE, code = xfun::read_utf8("an_gs_scripts/fun_calc_an_gs.R"), class.source = "fold-hide"}
```
calc_fun_Photosynthesis_StomatalConductance.R
```{r, eval = FALSE, code = xfun::read_utf8("an_gs_scripts/calc_fun_Photosynthesis_StomatalConductance.R"), class.source = "fold-hide"}
```
satvap.R
```{r, eval = FALSE, code = xfun::read_utf8("an_gs_scripts/satvap.R"), class.source = "fold-hide"}
```
LeafBoundaryLayer.R
```{r, eval = FALSE, code = xfun::read_utf8("an_gs_scripts/LeafBoundaryLayer.R"), class.source = "fold-hide"}
```
PAR.R
```{r, eval = FALSE, code = xfun::read_utf8("an_gs_scripts/PAR.R"), class.source = "fold-hide"}
```
hybrid_root_ci.R
```{r, eval = FALSE, code = xfun::read_utf8("an_gs_scripts/hybrid_root_ci.R"), class.source = "fold-hide"}
```
brent_root_ci.R
```{r, eval = FALSE, code = xfun::read_utf8("an_gs_scripts/brent_root_ci.R"), class.source = "fold-hide"}
```
CiFunc.R
```{r, eval = FALSE, code = xfun::read_utf8("an_gs_scripts/CiFunc.R"), class.source = "fold-hide"}
```
photosynthesis_calib.R
```{r, eval = FALSE, code = xfun::read_utf8("an_gs_scripts/photosynthesis_calib.R"), class.source = "fold-hide"}
```
setup_photosynthesis_calib.R
```{r, eval = FALSE, code = xfun::read_utf8("an_gs_scripts/setup_photosynthesis_calib.R"), class.source = "fold-hide"}
```
fun_costphoto.R
```{r, eval = FALSE, code = xfun::read_utf8("an_gs_scripts/fun_costphoto.R"), class.source = "fold-hide"}
```
fun_photosynthesis_calib.R
```{r, eval = FALSE, code = xfun::read_utf8("an_gs_scripts/fun_photosynthesis_calib.R"), class.source = "fold-hide"}
```

